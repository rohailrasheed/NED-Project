import { _ModuleSupport, type _Util } from 'ag-charts-community';
declare const BaseModuleInstance: typeof _ModuleSupport.BaseModuleInstance;
export interface PopoverConstructorOptions {
    detached?: boolean;
}
export interface PopoverOptions {
    ariaLabel?: string;
    class?: string;
    initialFocus?: HTMLElement;
    sourceEvent?: Event;
    onHide?: () => void;
}
/**
 * A non-modal element that overlays the chart.
 */
export declare abstract class Popover<Options extends PopoverOptions = PopoverOptions> extends BaseModuleInstance implements _ModuleSupport.ModuleInstance {
    protected readonly ctx: _ModuleSupport.ModuleContext;
    protected readonly hideFns: Array<() => void>;
    private readonly moduleId;
    private readonly element;
    private lastFocus?;
    constructor(ctx: _ModuleSupport.ModuleContext, id: string, options?: PopoverConstructorOptions);
    attachTo(popover: Popover): void;
    hide(opts?: {
        lastFocus?: null | undefined;
    }): void;
    protected removeChildren(): void;
    protected showWithChildren(children: Array<HTMLElement>, options: Options): HTMLDivElement;
    protected getPopoverElement(): HTMLDivElement | undefined;
    protected updatePosition(position: _Util.Vec2): void;
}
export {};
