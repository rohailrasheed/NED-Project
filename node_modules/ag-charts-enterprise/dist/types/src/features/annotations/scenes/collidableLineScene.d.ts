import { _Scene } from 'ag-charts-community';
export type ShapeClipMask = {
    x: number;
    y: number;
    radius: number;
};
export declare class CollidableLine extends _Scene.Line {
    collisionBBox?: _Scene.BBox;
    private readonly growCollisionBox;
    protected clipMask?: ShapeClipMask;
    setProperties<T>(styles: {
        [K in keyof T]?: T[K] | undefined;
    }, pickKeys?: (keyof T)[] | undefined): T;
    private updateCollisionBBox;
    isPointInPath(pointX: number, pointY: number): boolean;
    render(renderCtx: _Scene.RenderContext): void;
    setClipMask(mask?: ShapeClipMask): void;
    /**
     * Apply a clipping mask to the shape, this must be called before the shape calls `ctx.beginPath()`.
     */
    protected applyClipMask(ctx: any): void;
    protected closeClipMask(ctx: any): void;
}
