import type { _Scene, _Util } from 'ag-charts-community';
import { type AnnotationContext, AnnotationType } from '../annotationTypes';
import type { ShapePointProperties } from '../properties/shapePointProperties';
import { PointScene } from './pointScene';
export declare abstract class ShapeScene<Datum extends ShapePointProperties> extends PointScene<Datum> {
    type: AnnotationType;
    protected abstract shape: _Scene.Marker;
    constructor();
    update(datum: Datum, context: AnnotationContext): void;
    private updateShape;
    protected updateShapeStyles(datum: Datum): void;
    protected updateShapePath(datum: Datum, point: _Util.Vec2): void;
    containsPoint(x: number, y: number): boolean;
}
